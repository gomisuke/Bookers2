if (location.pathname != '/books/<%= @book.id %>') {
	$('#book_<%= @book.id %>').html("<%= j(render 'books/books', { book: @book }) %>")
} else {
	$('#book_show').html("<%= j(render 'books/book_show', { post: @book }) %>")
}